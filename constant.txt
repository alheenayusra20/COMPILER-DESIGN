/* Filename: constants.l */

%{
#include <stdio.h>
%}

%option noyywrap

%%
[0-9]+(\.[0-9]+)?([eE][+-]?[0-9]+)?   { printf("Constant: %s\n", yytext); }  
.|\n                                  { /* ignore everything else */ }
%%

int main() {
    yylex();
    return 0;
}
